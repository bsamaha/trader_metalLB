apiVersion: v1
kind: Service
metadata:
  name: k3s-api-server
  namespace: kube-system
  annotations:
    metallb.universe.tf/allow-shared-ip: k3s-api
spec:
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 6443
      targetPort: 6443
  selector:
    app: kube-apiserver